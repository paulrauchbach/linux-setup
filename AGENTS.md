# Repository Location

- Always make changes in the development repository at `/home/paul/dev/linux-setup`.
- Never edit `/home/paul/.local/share/linux-setup` directly. It is the checked-out version used to manage the installation, not the development workspace.
- Treat installed or generated copies as derived state. Update their source in the development repository instead.
